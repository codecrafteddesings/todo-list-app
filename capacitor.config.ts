import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codecrafted.todolistpro',
  appName: 'TodoList Pro',
  webDir: 'build'
};

export default config;
